<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ravneet.ieeedtu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.BiweeklyArticle" />
        <activity android:name=".activities.IEEECouncilActivity" />
        <activity android:name=".activities.NotificationActivity" />
        <activity
            android:name=".activities.MemberOnly"
            android:label="@string/title_activity_member_only"
            android:theme="@style/AppTheme" />
        <activity android:name=".PrivateActivity.SIGNotification" />
        <activity android:name=".PrivateActivity.EventNotification" />
        <activity android:name=".PrivateActivity.ChatRoom" />
        <activity android:name=".activities.ContactUs"></activity>
    </application>

</manifest>